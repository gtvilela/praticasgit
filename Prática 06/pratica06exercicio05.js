  
        var numeroMin = parseInt(prompt("Informe o menor número: "));
        var numeroMax = parseInt(prompt("Informe o maior número: "));    
        var soma = 0;

        for (i=numeroMin; i <= numeroMax; i++) {
   soma = soma + i;

}

       document.write(soma);
